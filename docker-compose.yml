services:
  copilot-go:
    build: .
    ports:
      - "3180:3000"
      - "4141:4141"
    volumes:
      - copilot-data:/root/.local/share/copilot-api
    restart: unless-stopped

volumes:
  copilot-data: